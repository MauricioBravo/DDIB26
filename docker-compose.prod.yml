services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    # env_file: .env.production   # uncomment once Firebase/Cloudinary/Blockfrost credentials exist
